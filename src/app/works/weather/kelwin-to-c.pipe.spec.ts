import { KelwinToCPipe } from './kelwin-to-c.pipe';

describe('KelwinToCPipe', () => {
  it('create an instance', () => {
    const pipe = new KelwinToCPipe();
    expect(pipe).toBeTruthy();
  });
});
